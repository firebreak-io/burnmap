{
  "name": "@burnmap/shoot",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": { "burnmap-shoot": "dist/cli.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@burnmap/parser": "*",
    "@burnmap/web": "*",
    "playwright": "^1.60.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^4.1.8",
    "tsx": "^4.19.0"
  }
}
